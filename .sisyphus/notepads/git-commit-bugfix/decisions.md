## Commit Strategy
- Chose to group the implementation (`app/storage.py`) and its regression test (`tests/test_api.py`) into a single atomic commit to ensure the fix and its verification are coupled.
